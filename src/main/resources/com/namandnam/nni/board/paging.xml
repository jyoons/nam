<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.paging">

  <sql id="pagingOpen">
    SELECT B.* FROM ( SELECT ROWNUM AS RNUM, A.* FROM (
  </sql>

  <sql id="pagingClose">
    ) A WHERE ROWNUM <![CDATA[<=]]> #{currentPageNo} * #{recordCountPerPage} ) B WHERE B.RNUM >= (#{currentPageNo} - 1) * #{recordCountPerPage} + 1
  </sql>

</mapper>